# READY opens immediately in tests; countdown and game-end remain shortened
poker.round-end.display-delay-ms=0
poker.ready-countdown-ms=900
poker.game-end.display-delay-ms=500

# Secure test secret for JWT
app.jwt.base64-secret=${JWT_SECRET}
# Disable rate limiting in tests
poker.rate-limiting.enabled=false
